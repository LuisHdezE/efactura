namespace ApplicationCore.Automapper
{
    /// <summary>
    /// Temporary marker retained only to preserve the legacy WebApi composition call while
    /// AutoMapper is removed from the dependency graph. It contains no mapping configuration.
    /// </summary>
    public sealed class AutomapperProfiles
    {
    }
}
